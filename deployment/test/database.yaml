version: "3"

volumes:
  auth-server-test-db:
  auth-server-test-redis:

services:
  auth-server-test-redis:
    image: bitnami/redis:7.4
    container_name: auth-server-test-redis
    hostname: auth-server-test-redis
    environment:
      - REDIS_PASSWORD=dev
    volumes:
      - auth-server-test-redis:/bitnami/redis/data
    ports:
      - 6378:6379
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      retries: 6
      start_period: 1s

  auth-server-test-db:
    image: bitnami/postgresql:16
    container_name: auth-server-test-db
    hostname: auth-server-test-db
    restart: unless-stopped
    environment:
      - POSTGRESQL_DATABASE=authserver
      - POSTGRESQL_USERNAME=dev
      - POSTGRESQL_PASSWORD=dev
      - POSTGRESQL_MAX_CONNECTIONS=200
    volumes:
      - auth-server-test-db:/bitnami/postgresql
    ports:
      - 5431:5432
    healthcheck:
      test: pg_isready -U postgres
      interval: 5s
      retries: 6
      start_period: 1s
